<section class="p-4">
  <h1 class="text-2xl font-bold mb-4">🔥 Drama Populer</h1>

  <div class="grid grid-cols-2 md:grid-cols-5 gap-4">
    <% dramas.forEach(d => { %>
      <a href="/watch/<%= d.shortPlayId %>" data-aos="fade-up">
        <img loading="lazy" class="rounded-lg" src="<%= d.shortPlayCover %>">
        <p class="mt-1 text-sm"><%= d.shortPlayName %></p>
      </a>
    <% }) %>
  </div>
</section>
