<div class="space-y-5">
  <div class="rounded-3xl border border-white/10 bg-white/5 p-5">
    <div class="text-xl font-semibold">Search</div>
    <div class="mt-1 text-sm text-white/60">Query: <span class="text-white"><%= q || "-" %></span></div>
    <div class="mt-1 text-xs text-white/50">Total: <%= total || 0 %></div>
  </div>

  <div id="searchGrid" class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-6 gap-3"
       data-infinite-grid
       data-items="<%= encodeURIComponent(JSON.stringify(list || [])) %>">
  </div>
</div>

<script>
  window.__SEARCH_READY__ = true;
</script>
